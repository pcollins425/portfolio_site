# Copied into Cloudflare Pages → Environment variables for **Production** builds, or rename to
# `.env.production` locally when running `npm run build`.
# No trailing slash.
VITE_API_BASE_URL=https://api.collinsmediallc.com
