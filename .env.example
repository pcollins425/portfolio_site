# Optional. Most config lives in backend_live/.env (single file).
#
# Compose reads this file only if you put it here AND run plain `docker compose` without
# --env-file. Prefer backend_live/.env +:
#   docker compose --env-file backend_live/.env up -d --build
#
# Or use the default sibling mount (../cursor-assistant) with no extra env at all.
