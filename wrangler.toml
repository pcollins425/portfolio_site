# Workers static assets — only **`pages_publish/`** is uploaded (never the whole repo).
# **`name`** must equal your Worker name in Workers & Pages, and Wrangler allows only lowercase letters, digits, and dashes.
name = "collinsmediallc-www"

compatibility_date = "2026-05-27"

[assets]
directory = "./pages_publish"
